[x] ~$0.00 18 minutes by GitHub Copilot `gpt-5.4`

[✨🔄] Add AI Agent which can help

- Agent should be able to answer questions about all the data that is currently in the app, including company profile, tax documents, and computed summaries.
- Agent should be able to edit, add, and delete tax documents based on user instructions from the chat
- For this use the `tools`
- Agent should comunicate only in Czech language and the instruction shoul be with diacritics
- The prompts and system prompts should be using `prompt` notation from `@promptbook/utils` for better readability and maintainability and security
- Agent should be in a floating bubble on the bottom right corner of the app, and should be accessible from any page
- Use OpenAI Agents SDK


```typescript

import { prompt } from '@promptbook/utils';

const promptString = prompt`
    Correct the following sentence:

    > ${unsecureUserInput}
`;

```